#include "main.h"
#include <stdio.h>

/**
 * mul - Multiplies 2 integers
 * @a: Integer to be multiplied with Integer b
 * @b: Integer to be multiplied with Integer a
 * Return: Return the result of the multiplication operation
 */
int mul(int a, int b)
{
	return (a * b);
}
